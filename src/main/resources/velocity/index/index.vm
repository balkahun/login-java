#parse("/velocity/layout.vm")
#@mainLayout()
    <h1>HEADING</h1>
    <h2>REGISTERED_USERS, $users.size()</h2>
    <ul>
        #foreach($user in $users)
            <li>$user</li>
        #end
    </ul>
    <p>PASSWORD_INFO</p>
#end